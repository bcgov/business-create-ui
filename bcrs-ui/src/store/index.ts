import Vue from 'vue'
import Vuex from 'vuex'

// State
import { stateText } from './state'

// Actions

// Mutations

// Getters

Vue.use(Vuex)

export const store = new Vuex.Store({
  state: {
    stateText
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
